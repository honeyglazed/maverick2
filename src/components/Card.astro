---
import { Image } from "astro:assets";
import type { ImageMetadata } from "astro";

export interface Props {
	title: string;
	img: ImageMetadata;
	body: string;
	href: string;
}

const { href, title, body, img } = Astro.props;
---

<div class="card relative hover:scale-105 ease-out duration-300">
	<Image src={img} alt={"at"} width={500} />
	<div class="m-2 z-10 flex place-items-end inset-0 absolute transition-opacity opacity-0 hover:opacity-100">
		<p
			class="grow rounded-lg p-4 text-center text-black font-sans text-xl bg-white/50"
		>
			{title}
		</p>
	</div>
</div>
